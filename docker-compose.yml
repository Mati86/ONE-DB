version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: roadm-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save "" --appendonly yes
    restart: unless-stopped
    networks:
      - roadm-network

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: roadm-redis-gui
    ports:
      - "8081:8081"
    environment:
      - REDIS_HOSTS=local:redis:6379
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - roadm-network

volumes:
  redis_data:

networks:
  roadm-network:
    driver: bridge 